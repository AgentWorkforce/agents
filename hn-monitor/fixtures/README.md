# HN monitor fixtures

These HTTP responses are the agent-local copies used by the platform case
files in `../cases/`. The legacy eval runner continues to use the matching
files under `evals/seeds/` until the platform runner reaches parity.
